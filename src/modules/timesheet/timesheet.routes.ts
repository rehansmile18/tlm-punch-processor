import { Router } from "express";

// TODO(phase: timesheet finalization): GET /timesheets, GET /timesheets/:id,
// GET /timesheets/:id/audit-trail, POST /timesheets/:id/reprocess, POST /timesheets/:id/void.
// Placeholder so app.ts resolves while earlier phases land first.
export const timesheetRouter = Router();
