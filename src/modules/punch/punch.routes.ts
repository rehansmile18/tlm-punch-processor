import { Router } from "express";

// TODO(phase: punch ingestion): single + bulk punch submission, list, correction. Placeholder so
// app.ts resolves while the master-data phase lands first.
export const punchRouter = Router();
